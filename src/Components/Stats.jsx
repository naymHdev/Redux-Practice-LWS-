/* eslint-disable react/prop-types */


const Stats = ({total}) => {
    return (
        <div>
            <p className=" text-3xl font-bold text-center py-6">Total: {total}</p>
        </div>
    );
};

export default Stats;